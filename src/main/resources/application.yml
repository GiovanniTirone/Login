spring:
  datasource:
    url: jdbc:mysql://localhost:3306/login
    username: root
    password: Stellari92!
    driverClassName: com.mysql.cj.jdbc.Driver
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
      jakarta:
        persistence:
          sharedCache:
            mode: ENABLE_SELECTIVE
  mail:
    host: smtp.sendgrid.net
    port: 587
    username: tironegiovanni4@gmail.com
    password: GhanBuriGhan92!?
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  jackson:
    serialization:
      fail-on-empty-beans: false

